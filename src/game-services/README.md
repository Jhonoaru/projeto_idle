# Game Services

Future home for application services that coordinate the engine, persistence,
settings, save/load operations, and desktop integration.

Keep this layer thin so gameplay rules remain in `game-engine`.
